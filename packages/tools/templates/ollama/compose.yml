services:
  ollama:
    image: ollama/ollama:0.13.0
    restart: unless-stopped
    ports:
      - '${OLLAMA_HOST_PORT:-11434}:11434'
    volumes:
      - ollama-data:/root/.ollama

volumes:
  ollama-data:
