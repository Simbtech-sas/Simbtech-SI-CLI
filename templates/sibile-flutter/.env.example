# Passed with --dart-define, not read from a file: anything compiled into a
# mobile binary is readable by anyone who has the binary. No secrets here.
#   flutter run --dart-define=API_URL=http://10.0.2.2:8080
API_URL=http://localhost:8080
