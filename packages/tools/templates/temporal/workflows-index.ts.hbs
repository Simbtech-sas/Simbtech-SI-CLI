/**
 * The workflow bundle's entry point.
 *
 * Temporal bundles everything reachable from this file and runs it in an
 * isolated, deterministic sandbox — no Node built-ins, no network. Export
 * workflows here; keep everything else out, because anything imported from here
 * is pulled into that sandbox and will fail at bundle time if it touches I/O.
 */
export * from './onboarding.workflow';

// si:workflows
