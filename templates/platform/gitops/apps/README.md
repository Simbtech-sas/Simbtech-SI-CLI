# gitops/apps

One directory per service, generated by `si service add <name>`.

Argo watches this path recursively and prunes what is removed, so deleting a
directory here undeploys the service. Do not hand-edit: regenerate instead, or
the next `si service add` will disagree with what is running.
