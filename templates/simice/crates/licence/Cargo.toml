[package]
name = "licence"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Offline licence issuing and verification. No Tauri, no I/O — pure logic so it can be tested anywhere."

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
ed25519-dalek = { version = "3.0.0", features = ["rand_core"] }
base64 = "0.23.1"
hex = "0.4.3"

[dev-dependencies]
rand = "0.10.2"
ed25519-dalek = { version = "3.0.0", features = ["rand_core"] }
